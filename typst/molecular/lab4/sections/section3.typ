= Порядок выполнения работы

1. Записать номера ячеек в табл. 1 так, чтобы центральная ячейка имела номер $i = 0$, а остальные изменялись от $i = -k$ до $i = k$.

2. Засыпать в воронку сыпучий материал.
3. Измерить высоты заполнения ячеек $ell_(i 1)$. Результаты измерений занести в табл. 1.
4. Рассчитать значения вероятности $P_(1)(i)$ по формуле
$ P_(i)(i) = frac(ell_(i 1), sum_(i=-k)^(k) ell_(i 1)) $
Результаты вычислений занести в табл. 1.
5. Рассчитать значения параметра распределения $sigma_(1)$ по формуле
$ sigma_(1)^(2) = frac(sum_(i=-k)^(k) i^(2) dot ell_(i 1), sum_(i=-k)^(k) ell_(i 1)) $
Результаты вычислений занести в табл. 1.
6. Рассчитать значения функции распределения $rho_(1)(i, sigma)$ по формуле
$ rho_(1)(i, sigma) = frac(1, sigma_(1) sqrt(2 pi)) dot e^(-frac(i^(2), 2 sigma_(1)^(2))) $
Результаты вычислений занести в табл. 1.
7. Открыть бункер 3 (см. @img2) и освободить его от сыпучего материала.
8. Записать номера ячеек в табл. 2 так, чтобы центральная ячейка имела номер $i = 0$, а остальные изменялись от $i = -k$ до $i = k$.
9. Измерить высоты заполнения ячеек $ell_(i 2)$. Результаты измерений занести в табл. 2.
10. Рассчитать значения вероятности $P_(2)(i)$ по формуле
$ P_(2)(i) = frac(ell_(i 2), sum_(i=-k)^(k) ell_(i 2)) $
Результаты вычислений занести в табл. 2.
11. Рассчитать значения параметра распределения $sigma_(2)$ по формуле
$ sigma_(2)^(2) = frac(sum_(i=-k)^(k) i^(2) dot ell_(i 2), sum_(i=-k)^(k) ell_(i 2)) $
Результаты вычислений занести в табл. 2.
12. Рассчитать значения функции распределения $rho_(2)(i, sigma)$ по формуле
$ rho_(2)(i, sigma) = frac(1, sigma_(2) sqrt(2 pi)) dot e^(-frac(i^(2), 2 sigma_(2)^(2))) $
Результаты вычислений занести в табл. 2.
13. Открыть бункер 3 (см. @img2) и освободить его от сыпучего материала.
14. Рассчитать среднее значение вероятности по формуле среднего арифметического, взяв для вычислений значения $P_(1)$ и $P_(2)$ из табл. 1 и табл. 2 соответственно. Результаты занести в табл. 3.
15. Построить график зависимости $P_(c p)$ от номера ячейки $i$.
16. Вычислить среднее значение функции распределения $rho_(c p)(i, sigma)$ по формуле среднего арифметического, взяв для вычислений значения $rho_(1)$ и $rho_(2)$ из табл. 1 и табл. 2 соответственно. Результаты занести в табл. 3.
17. Построить график зависимости функции распределения $rho_(c p)$ от номера ячейки $i$.

