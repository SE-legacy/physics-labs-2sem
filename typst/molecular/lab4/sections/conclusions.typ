= Выводы

== Вывод 1:

== Вывод 2:
В ходе выполнения лабораторной работы были исследованы статистические закономерности распределения сыпучего материала в ячейках. Проведены измерения высот заполнения ячеек $ℓ_i$, рассчитаны вероятности $P_1(i)$ и $P_2(i)$ по формуле. Анализ графиков показал, что распределение близко к нормальному распределению (гауссовскому). Наибольшие значения наблюдались в центральных ячейках ($i = 0$), а по мере удаления от центра значения уменьшались симметрично.  

Экспериментальные данные подтверждают, что распределение частиц подчиняется статистическим закономерностям, близким к нормальному распределению.

